# =====================================================
# SUPABASE ENVIRONMENT VARIABLES
# =====================================================
# Copy this file to .env.local and fill in your values
# Get these from your Supabase project settings
# =====================================================

# Supabase Project URL
# Find at: Project Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon/Public Key
# Find at: Project Settings > API > Project API keys > anon/public
# This key is safe to use in the browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (KEEP SECRET!)
# Find at: Project Settings > API > Project API keys > service_role
# This key has admin privileges - NEVER expose to the browser
# Only use in server-side code
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =====================================================
# OPTIONAL: Local Development with Supabase CLI
# =====================================================
# If running Supabase locally with `supabase start`
# NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key

# =====================================================
# NOTES
# =====================================================
# - The NEXT_PUBLIC_ prefix makes variables available in the browser
# - Service role key should ONLY be used in server-side code
# - Never commit .env.local to version control
# - Restart dev server after changing environment variables
